CREATE TABLE [dbo].[ContainerLayout] (
    [ContainerLayoutID]   TINYINT      IDENTITY (1, 1) NOT NULL,
    [ContainerLayoutName] VARCHAR (40) NOT NULL,
    PRIMARY KEY CLUSTERED ([ContainerLayoutID] ASC)
);

